<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TestsApplication.Views.TestListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestsApplication.Views"
    xmlns:models="using:TestsApplication.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="TestItemTemplate" x:DataType="models:Examen">
            <ItemContainer CornerRadius="{StaticResource OverlayCornerRadius}">
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource OverlayCornerRadius}" Width="300">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="120"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Examen" Margin="8,0" Foreground="{ThemeResource TextFillColorDisabled}"/>
                    <TextBlock Text="{x:Bind Titulo}" Margin="12,0" TextWrapping="Wrap" TextTrimming="WordEllipsis" FontSize="20" Grid.Row="1"/>
                    <Border Grid.Row="2" Padding="8,6,0,6" Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                        <TextBlock Text="{x:Bind FechaDeCreacion.ToString()}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}"/>
                    </Border>
                </Grid>
            </ItemContainer>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <CommandBar.Content>
                <TextBlock Text="Lista de examenes" Margin="16,8" 
                   Style="{ThemeResource SubtitleTextBlockStyle}"/>
            </CommandBar.Content>
        </CommandBar>

        <GridView x:Name="TestItemView" ItemsSource="{x:Bind TestsList}" ItemTemplate="{StaticResource TestItemTemplate}"
           Grid.Row="1" Padding="16" SelectionMode="None" ItemClick="TestItemView_ItemClick" IsItemClickEnabled="True">
        </GridView>
    </Grid>
</Page>
